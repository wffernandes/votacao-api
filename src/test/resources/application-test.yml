spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true

  web:
    error:
      include-stacktrace: never

integracao:
  elegibilidade:
    base-url: http://localhost:9999
    timeout-segundos: 3