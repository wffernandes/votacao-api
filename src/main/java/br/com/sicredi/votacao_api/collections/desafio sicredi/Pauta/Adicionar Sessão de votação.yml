info:
  name: Adicionar Sessão de votação
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/v1/pautas/1/sessoes
  body:
    type: json
    data: |-
      {
        "duracaoMinutos": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
