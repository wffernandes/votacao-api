info:
  name: Adicionar voto
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/v1/pautas/3/votos
  body:
    type: json
    data: |-
      {
        "associadoId": "ASSOC-003",
        "opcao": "NAO"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
